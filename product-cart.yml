spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jfsseptkart
    username: root
    password: root		

#jpa specific properties
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

message: this is cart service profile