spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jfsseptkart
    username: root
    password: root		

#jpa specific properties
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

message: in production profile updated refresh scope testing updated
